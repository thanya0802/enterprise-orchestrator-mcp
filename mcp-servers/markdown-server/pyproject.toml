[project]
name = "markdown-server"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "mcp>=1.0.0",
    "chromadb>=0.5.0",
    "langchain-text-splitters>=0.3.0",
]

[project.scripts]
markdown-server = "server:main"
